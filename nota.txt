

const [alumnoEditar, setAlumnoEditar] = useState(null);
const [showRegistroForm, setShowRegistroForm] = useState(true);



///////////////////////////// 

  const actualizarAlumno = async (id) => {
    try {
      const alumnoActualizado = {
        id,
        nombre_alumno: nombre,
        email_alumno: email,
        curso_alumno: curso,
        sexo_alumno: sexo,
        habla_ingles: hablaIngles ? "true" : "false",
      };

      const response = await axios.put(URL_API, alumnoActualizado);
      console.log("Alumno actualizado:", response.data);
      // Actualizar la lista de alumnos
      obtenerAlumnos();
    } catch (error) {
      console.error("Error al actualizar alumno:", error);
    }
  };


